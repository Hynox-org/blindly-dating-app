# AI/ML Components

This directory contains the AI and Machine Learning components for the Blindly app.

## Structure

- `aws_rekognition/`: Scripts and lambdas for AWS Rekognition integration (Profile Verification).
- `models/`: TensorFlow Lite models for on-device inference (if applicable).

## Setup

(Add setup instructions for AWS credentials and Python environment here)
